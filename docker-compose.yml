version: '3.8'

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: projector-image-server
    environment:
      - PORT=3001
      - NODE_ENV=${NODE_ENV:-production}
    expose:
      - "3001"
    restart: unless-stopped

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: projector-image-client
    expose:
      - "80"
    depends_on:
      - server
    restart: unless-stopped